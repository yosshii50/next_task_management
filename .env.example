DATABASE2_POSTGRES_URL=""
DATABASE2_PRISMA_DATABASE_URL=""
DATABASE2_DATABASE_URL=""

NEXTAUTH_SECRET="please-set-a-secure-secret"
NEXTAUTH_URL="http://localhost:3000"
APP_BASE_URL="http://localhost:3001"

# SMTP設定（親承認メール送信用）
# SMTPサーバーのホスト名（例: smtp.gmail.com や smtp.sendgrid.net）
SMTP_HOST=""
# SMTPサーバーのポート番号（TLS開始方式なら 587、SSLなら 465）
SMTP_PORT="587"
# SMTP認証に使うユーザー名（アカウントのメールアドレスやユーザーID）
SMTP_USER=""
# SMTP認証に使うパスワードまたはAPIキー
SMTP_PASSWORD=""
# SMTPS(465)で暗号化を強制する場合は true、STARTTLS(587)なら false
SMTP_SECURE="false"
# 送信元として表示するメールアドレス（指定がなければ SMTP_USER が使われます）
MAIL_FROM=""

# 未使用
OHANASHIMK-KEY=""
